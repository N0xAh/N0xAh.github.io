{
  "name": "portfolio-cybersecurity",
  "version": "1.0.0",
  "description": "Jekyll Cybersecurity Portfolio with Tailwind CSS",
  "scripts": {
    "build:css": "npx tailwindcss -i ./assets/css/styles.css -o ./assets/css/main.css --minify",
    "watch:css": "npx tailwindcss -i ./assets/css/styles.css -o ./assets/css/main.css --watch",
    "dev": "npm run watch:css & bundle exec jekyll serve --livereload",
    "build": "npm run build:css && bundle exec jekyll build"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32"
  }
}
